services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pages
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/Cooking/scratch/giggle/crawler/data

volumes:
  postgres_data:
